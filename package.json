{
  "name": "blogsphere",
  "version": "1.0.0",
  "scripts": {
    "build": "cd frontend && npm install && CI=false npm run build"
  },
  "dependencies": {
    "react": "^18.2.0",
    "react-dom": "^18.2.0"
  }
}